# GitHub Workflows

This folder contains GitHub Actions workflows for automated testing and building.

## Workflows

- **test.yml** - Runs Selenium tests on every push
- **build.yml** - Verifies production build on every push
